---
# tasks file for ans_loginsight_bootstrap_role
- name: Include OS-specific variables.
  include_vars: "{{ ansible_distribution }}.yml"

- name: Include OS-specific tasks.
  include_tasks: "{{ ansible_distribution }}.yml"
